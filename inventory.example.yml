---
# OpenOctopus inventory
# Copy this file to inventory.yml and edit with your server details.

all:
  children:
    octopus:
      hosts:
        my-server:
          ansible_host: 192.168.1.100
          ansible_user: deploy
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519
          # ansible_port: 22
